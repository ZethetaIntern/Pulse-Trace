# Server
PORT=4000
NODE_ENV=development

# Database
DATABASE_URL=postgresql://pulsetrace:pulsetrace@localhost:5432/pulsetrace

# Redis
REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=info

# CORS
# Comma-separated list of allowed origins. In development, defaults to '*' if not set.
# In production, this must be explicitly configured.
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
