// Phase 1 placeholder for the database layer.
//
// schema.prisma intentionally contains no models yet — database models belong to
// the next phase. Until then, `prisma generate` emits no client, so PrismaClient
// cannot be imported from '@prisma/client'. Restore this once models are added:
//
//   import { PrismaClient } from '@prisma/client';
//   export const prisma = new PrismaClient();
export {};
